/**
 * Backward, forward and full compatibility verdicts.
 *
 * Orientation follows the schema-registry convention: BACKWARD is reader = proposed,
 * writer = baseline; FORWARD is the reverse.
 */
package com.contractguard.compatibility;
