package com.contractguard.risk;

/** Operational risk rules. One implemented so far. */
public enum RiskRuleId {

    /**
     * A new enum symbol can resolve to the old enum's default for an older consumer, and that
     * consumer treats the default symbol as a meaningful business state.
     */
    ENUM_SEMANTIC_FALLBACK_RISK
}
